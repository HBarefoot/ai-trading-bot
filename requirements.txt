# Streamlit Cloud specific requirements
# Lighter weight, no PostgreSQL, no heavy ML libraries

# Frontend (Required)
streamlit==1.28.2
plotly==5.17.0
pandas==2.1.3
numpy>=1.26.0,<2.0.0

# API Client (Required)
requests==2.31.0

# Optional: For local development simulation
# These are NOT needed on Streamlit Cloud since it's frontend only
